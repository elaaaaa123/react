
import React from 'react';

const Logo = ({ src, alt }) => {
  return (
    
    <img 
      src={src} 
      alt={alt} 
      style={{ width: '116.07px', height: '38.21px' }} 
    />
  );
};

export default Logo;
